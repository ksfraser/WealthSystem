# SRP Navigation Architecture Diagram

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                   Navigation Architecture                    │
└─────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │ config/         │
                    │ navigation.php  │
                    │                 │
                    │ - Display mode  │
                    │ - Roles config  │
                    │ - Cache settings│
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │ NavigationFactory│
                    │                 │
                    │ - Creates       │
                    │   builders      │
                    │ - Loads config  │
                    │ - Registers     │
                    │   providers     │
                    └────────┬────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
    ┌───────────▼──────────┐   ┌─────────▼────────────┐
    │ NavigationBuilder    │   │ DashboardCardBuilder │
    │                      │   │                      │
    │ - Builds menus       │   │ - Builds cards       │
    │ - Filters by access  │   │ - Filters by access  │
    │ - Detects active     │   │ - Renders HTML       │
    │ - Renders HTML       │   │                      │
    └──────────┬───────────┘   └──────────┬───────────┘
               │                           │
               └───────────┬───────────────┘
                           │
                    ┌──────▼──────┐
                    │  Providers   │
                    │  (consume)   │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
┌───────▼────────┐ ┌───────▼────────┐ ┌──────▼─────────┐
│PortfolioItems  │ │StockAnalysis   │ │ AdminItems     │
│Provider        │ │ItemsProvider   │ │ Provider       │
│                │ │                │ │                │
│- getMenuItems()│ │- getMenuItems()│ │- getMenuItems()│
│- getDashboard  │ │- getDashboard  │ │- getDashboard  │
│  Cards()       │ │  Cards()       │ │  Cards()       │
└───────┬────────┘ └───────┬────────┘ └────────┬───────┘
        │                  │                    │
        └──────────────────┼────────────────────┘
                           │
                    ┌──────▼──────┐
                    │   Models     │
                    │  (return)    │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
┌───────▼────────┐ ┌───────▼────────┐ ┌──────▼─────────┐
│NavigationItem  │ │DashboardCard   │ │MenuItem        │
│(abstract base) │ │(extends Nav.)  │ │(extends Nav.)  │
│                │ │                │ │                │
│- id            │ │+ actions[]     │ │+ children[]    │
│- title         │ │+ cardType      │ │+ badge         │
│- icon          │ │+ colorClass    │ │+ dropdown      │
│- url           │ │                │ │                │
│- requiredRole  │ │- render()      │ │- render()      │
│- hasAccess()   │ │                │ │- renderDropdown│
└────────────────┘ └────────────────┘ └────────────────┘
```

## Data Flow

```
User Request
     │
     ▼
┌────────────────┐
│  dashboard.php │
│  OR            │
│  Navigation    │
│  Header        │
└────────┬───────┘
         │
         │ 1. Get current user
         ▼
┌────────────────┐
│  UserAuthDAO   │
│                │
│ - getCurrentUser()
│ - isAdmin()    │
└────────┬───────┘
         │
         │ 2. Create builder
         ▼
┌────────────────────┐
│NavigationFactory   │
│                    │
│createNavigationBuilder(user)
│   OR               │
│createDashboardCardBuilder(user)
└────────┬───────────┘
         │
         │ 3. Register providers
         ▼
┌────────────────────┐
│  Providers         │
│                    │
│- PortfolioItems    │
│- StockAnalysis     │
│- Admin             │
│- etc...            │
└────────┬───────────┘
         │
         │ 4. Get items
         ▼
┌────────────────────┐
│  NavigationItems   │
│                    │
│- MenuItem objects  │
│- DashboardCard     │
│  objects           │
└────────┬───────────┘
         │
         │ 5. Filter by access
         ▼
┌────────────────────┐
│  Access Check      │
│                    │
│- hasAccess(user)   │
│- requiredRole      │
│- isAdmin check     │
└────────┬───────────┘
         │
         │ 6. Apply display mode
         ▼
┌────────────────────┐
│  Display Logic     │
│                    │
│IF hidden mode:     │
│  - Skip if no      │
│    access          │
│IF greyed_out mode: │
│  - Show disabled   │
│  - Add tooltip     │
└────────┬───────────┘
         │
         │ 7. Render HTML
         ▼
┌────────────────────┐
│  HTML Output       │
│                    │
│- Menu items        │
│- Dashboard cards   │
│- Dropdowns         │
└────────────────────┘
```

## Access Control Flow

```
┌────────────────────────────────────────┐
│         Check User Access              │
└────────────────────────────────────────┘

NavigationItem.hasAccess(userRole, isAdmin)
              │
              ▼
┌─────────────────────────────┐
│ Is admin user?              │
└─────────────┬───────────────┘
              │
       ┌──────┴──────┐
       │             │
      YES           NO
       │             │
       ▼             ▼
   ┌───────┐    ┌──────────────────┐
   │GRANT  │    │Item requires role?│
   │ACCESS │    └────────┬──────────┘
   └───────┘             │
                  ┌──────┴──────┐
                  │             │
                 YES           NO (null)
                  │             │
                  ▼             ▼
          ┌──────────────┐  ┌───────┐
          │User role ==  │  │GRANT  │
          │required role?│  │ACCESS │
          └──────┬───────┘  └───────┘
                 │
          ┌──────┴──────┐
          │             │
         YES           NO
          │             │
          ▼             ▼
      ┌───────┐    ┌───────┐
      │GRANT  │    │DENY   │
      │ACCESS │    │ACCESS │
      └───────┘    └───────┘
                       │
                       ▼
              ┌────────────────┐
              │Display mode?   │
              └────────┬───────┘
                       │
                ┌──────┴──────┐
                │             │
              hidden      greyed_out
                │             │
                ▼             ▼
           ┌────────┐    ┌──────────┐
           │Don't   │    │Show but  │
           │render  │    │disabled  │
           └────────┘    │+ tooltip │
                         └──────────┘
```

## Example: Portfolio Menu Item Flow

```
1. Provider Creates Item
   PortfolioItemsProvider.getMenuItems()
        │
        └─► MenuItem(
              'portfolio',
              'Portfolio',
              'Portfolio management',
              '💼',
              '#',
              null,  ← No role required (available to all)
              1
            )

2. Builder Receives Item
   NavigationBuilder
        │
        ├─► Check access: hasAccess(user.role, user.isAdmin)
        │   └─► requiredRole = null → GRANTED
        │
        ├─► Mark active if matches current path
        │
        └─► Render HTML with dropdown children

3. Output
   <li class="nav-item dropdown">
     <a class="nav-link dropdown-toggle" href="#">
       💼 Portfolio
     </a>
     <ul class="dropdown-menu">
       <li><a href="MyPortfolio.php">🏠 My Portfolio</a></li>
       <li><a href="portfolios.php">📈 Manage Portfolios</a></li>
       ...
     </ul>
   </li>
```

## Example: Admin Item Flow (Restricted)

```
1. Provider Creates Item
   AdminItemsProvider.getMenuItems()
        │
        └─► MenuItem(
              'admin',
              'Administration',
              'Admin functions',
              '⚙️',
              '#',
              'admin',  ← Requires admin role
              5
            )

2. Builder Receives Item (Normal User)
   NavigationBuilder
        │
        ├─► Check access: hasAccess('user', false)
        │   └─► requiredRole = 'admin' → DENIED
        │
        ├─► Check display mode from config
        │   └─► restricted_items_mode = 'greyed_out'
        │
        └─► Render disabled with tooltip

3. Output (Greyed Out Mode)
   <li class="nav-item" style="opacity: 0.5;">
     <a class="nav-link disabled" href="#" 
        title="Requires admin access">
       ⚙️ Administration
     </a>
   </li>

4. Output (Hidden Mode)
   (nothing rendered)
```

## Class Responsibilities

```
┌──────────────────────────────────────────────┐
│ NavigationItem (Base Class)                  │
├──────────────────────────────────────────────┤
│ Responsibility:                              │
│ - Store navigation item data                 │
│ - Check access permissions                   │
│ - Provide data as array                      │
├──────────────────────────────────────────────┤
│ Methods:                                     │
│ - hasAccess(userRole, isAdmin): bool         │
│ - toArray(): array                           │
│ - Getters for all properties                 │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ DashboardCard (Extends NavigationItem)       │
├──────────────────────────────────────────────┤
│ Responsibility:                              │
│ - Represent dashboard card with actions      │
│ - Render card HTML                           │
├──────────────────────────────────────────────┤
│ Additional:                                  │
│ - actions[] (buttons)                        │
│ - cardType (styling)                         │
│ - render(hasAccess, mode): string            │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ MenuItem (Extends NavigationItem)            │
├──────────────────────────────────────────────┤
│ Responsibility:                              │
│ - Represent menu item with dropdown support  │
│ - Render menu HTML                           │
├──────────────────────────────────────────────┤
│ Additional:                                  │
│ - children[] (dropdown items)                │
│ - badge (notification badge)                 │
│ - render(hasAccess, mode): string            │
│ - renderDropdownItem(...): string            │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ NavigationItemProvider (Interface)           │
├──────────────────────────────────────────────┤
│ Responsibility:                              │
│ - Define contract for providers              │
├──────────────────────────────────────────────┤
│ Methods:                                     │
│ - getMenuItems(): MenuItem[]                 │
│ - getDashboardCards(): DashboardCard[]       │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ PortfolioItemsProvider (Implements Interface)│
├──────────────────────────────────────────────┤
│ Responsibility:                              │
│ - Provide portfolio navigation items         │
│ - Single source of truth for portfolio items │
├──────────────────────────────────────────────┤
│ Returns:                                     │
│ - Portfolio menu with 4 child items          │
│ - 4 dashboard cards                          │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ NavigationBuilder (Service)                  │
├──────────────────────────────────────────────┤
│ Responsibility:                              │
│ - Build navigation menus from providers      │
│ - Filter items by user access                │
│ - Detect active page                         │
│ - Render menu HTML                           │
├──────────────────────────────────────────────┤
│ Methods:                                     │
│ - addProvider(provider)                      │
│ - getMenuItems(): MenuItem[]                 │
│ - renderMenu(): string                       │
│ - getMenuArray(): array                      │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ DashboardCardBuilder (Service)               │
├──────────────────────────────────────────────┤
│ Responsibility:                              │
│ - Build dashboard cards from providers       │
│ - Filter cards by user access                │
│ - Render card HTML                           │
├──────────────────────────────────────────────┤
│ Methods:                                     │
│ - addProvider(provider)                      │
│ - getCards(): DashboardCard[]                │
│ - renderCards(): string                      │
│ - getCardsArray(): array                     │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ NavigationFactory (Factory)                  │
├──────────────────────────────────────────────┤
│ Responsibility:                              │
│ - Create pre-configured builders             │
│ - Load configuration                         │
│ - Register all providers automatically       │
├──────────────────────────────────────────────┤
│ Static Methods:                              │
│ - createNavigationBuilder(user, path)        │
│ - createDashboardCardBuilder(user)           │
│ - getProvider(className)                     │
└──────────────────────────────────────────────┘
```
